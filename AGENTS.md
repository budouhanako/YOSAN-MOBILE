# 项目规则

- 只修改用户当前明确提出的内容，不改无关功能和样式。
- 修改前先确认目标文件，存档文件未经允许不得修改。
- 用户需求统一维护在 `USER_REQUIREMENTS.md`，每次修改后同步更新。
- 新需求覆盖旧需求时，记录旧需求已废止。
- UI 修改不能影响跳转和已有功能。
- 修改后必须检查最终文件；没有实际点击测试时，要明确说明。
- 不得把“文件写入成功”当作“功能正常”。

