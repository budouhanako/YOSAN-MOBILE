# 用户需求

## 文件

- 当前试作：`archive/internal-budget-execution-create-20260820-mobile-try.html`
- 基准存档：`archive/internal-budget-execution-create-20260819.html`
- 未经明确要求，不修改桌面 `V1.0.html`。

## 通用要求

- 页面文案全部使用日语。
- 首页、数据一览、电子承认、创建页和详情页全体统一使用 `Meiryo` 字体，并保留 `sans-serif` 回退。
- 移动端保持轻量、易填写。
- 只改用户明确提出的内容，保留已确认功能和样式。
- 每次修改后检查跳转和相关按钮。

## 当前执行内容

- 番号左侧显示。
- 上へ、下へ、複製、クリア、削除在番号行右侧横向排列。
- 下一行显示整宽虚线按钮，用于 AI 读取发票文件。
- 文件读取按钮位于番号操作行下方并横向拉宽；图标使用四角扫描框内显示 `AI` 的样式，文案为 `ファイルから読取`。
- `執行内容` 数据块上方显示与单条文件读取按钮相同样式的 `一括ファイルから読取` 按钮，并支持选择多个文件。
- `一括ファイルから読取` 的 AI 扫描图标在四角扫描框基础上增加叠放文件轮廓，表达一括读取；单条读取图标保持原样。
- 批量图标的叠放文件轮廓使用较小尺寸和紧凑错位，避免与扫描框重叠显乱。
- ~~批量图标的文件轮廓进一步缩小并放在扫描框内部右上角，不得超出扫描框。~~ 已废止；位置调整为框内偏左下。
- 批量图标右上角文件轮廓使用更小尺寸，并采用与主扫描框一致的深紫色。
- 一括读取附件完成后，`執行内容` 不足 3 个数据块时自动补足为 3 个，并为前 3 个数据块生成示例数据。
- 输入区域保持完整宽度。
- `基本情報` Step 的 `備考` Panel 默认折叠。
- `支払方法・支払先情報` Panel 默认折叠，`支払方法` 默认选中 `振込`。
- 样式选择对话框默认选中 `009 New1`，仍允许用户改选其他样式。
- `執行内容` Step 名称改为 `執行内容・明細`。
- 原独立 `執行明細` Step 已废止，完整内容移到 `執行内容・明細` Step 最下方。
- `執行明細` 的 `執行内容から読込` 按钮采用 `基本情報` 的 `前回内容を読込` 按钮样式，不使用白色填充背景。
- `戻る` 返回首页。

## 交付要求

- 仓库提供 `index.html` 正式入口。
- 支持 `#home`、`#create`、`#detail` 直接访问。
- 最终封装文件必须通过 Chrome 实际加载验证。

## 電子承認画面

- 首页最右侧 `電子承認` Tab 可进入独立电子承认画面。
- 画面内显示 `未承認`、`承認済`、`差戻し・承認取消`、`申請` 4 个 Tab。
- 電子承認 Tab 使用扁平文字导航样式和底部分隔线，选中项使用系统紫色文字和紫色下划线。原“整体透明背景”要求已废止。
- 電子承認顶部 Tab 区域使用白色背景并降低高度，保持紫色选中下划线。
- 電子承認顶部 Tab 白色区域横向拉宽，抵消内容区左右边距并贯穿画面宽度。~~4 个 Tab 保持等宽。~~ 已废止：按名称长度分配宽度。
- ~~電子承認顶部 4 个 Tab 的字号为 `12px`。~~ 已废止：统一改为 `13px`。
- ~~数据按月和日期分组显示。~~ 已废止：日期标题不再显示。
- 数据只显示月份分组标题，不显示月份下方的每日日期标题。
- 数据块显示件名、金额、申请日，右上角显示电子承认状态。
- 数据块第一行显示 `申請番号：xxxxx`，与右侧电子承认状态 Label 对齐；第二行显示件名；第三行并排显示金额和申请日。
- 第三行中 `金額` Label 与金额值紧挨横向排列，`申請日` Label 与日期值紧挨横向排列，两组信息保持左右分栏。
- 数据块左侧显示种别图标和名称：`執行伺`、`入出金伺`、`旅費申請`、`旅費精算`。
- 种别图标使用较小尺寸，并在数据块左侧上下垂直居中。
- 种别图标和名称与卡片左边缘保持适当距离，整体稍向右排列。
- 金额 Label 和金额值左右排列；右上角电子承认状态使用更大字号。
- 右上角电子承认状态名称使用普通字重，不加粗。
- `未承認` 数据块下方显示 `承認`、`差戻し` 按钮。
- `承認`、`差戻し` 按钮使用更小尺寸，分别使用蓝色和红色。
- 画面底部显示 `一括承認` 按钮，并支持单条及批量审批交互。
- `一括承認` 按钮使用蓝色。
- ~~画面底部 `ホーム`、`データ一覧`、`電子承認` 主导航不使用选中背景色；选中后仅图标变为系统紫色高亮，文字颜色保持一致。~~ 已废止：选中文字也改为紫色。
- 底部主导航选中图标使用清晰的深紫色和较高字重，确保不被默认灰色覆盖。
- 底部主导航使用 `Meiryo` 字体。
- 电子承认数据块内文字不使用粗体，并改为更易读的深灰色。
- ~~`承認`、`差戻し` 按钮宽度缩为原来的一半，保持现有颜色和高度并靠右显示。~~ 已废止：改为分别与左右信息列对齐。
- 画面底部主导航 Tab 高度调小，选中图标颜色及跳转功能保持不变。
- `一括承認` 按钮所在底栏不显示白色背景，按钮本身保持蓝色。
- ~~`承認` 按钮与左侧金额列左对齐，`差戻し` 按钮与右侧申请日列右对齐；两个按钮各保持原按钮的一半宽度。~~ 已废止：`差戻し` 改为在申请日所在右列下方左对齐。
- ~~电子承认数据块正文统一使用 `10px` 字号，种别图标符号维持原尺寸。~~ 已废止：数据块文字统一改为 `12px`。
- `一括承認` 按钮高度调小，宽度、颜色及批量审批功能保持不变。
- ~~`承認`、`差戻し` 按钮内文字使用 `11px` 字号。~~ 已废止：按钮文字随数据块统一改为 `12px`。
- ~~电子承认顶部四个 Tab 使用普通字重，不加粗。~~ 已废止：仅选中 Tab 加粗。
- 电子承认画面上下滚动时，顶部标题栏和四个 Tab 固定显示，仅月份及数据块区域滚动。
- ~~申请日信息在右列内右对齐，使日期值（如 `08/19`）右边缘与 `差戻し` 按钮右边缘对齐。~~ 已废止：申请日恢复原位置，仅移动按钮。
- `未承認` 存在未处理数据时显示未读圆点；全部逐条处理或一括承認后自动隐藏。
- 申请日保持原位置；`差戻し` 按钮放在申请日所在右列的正下方并左对齐。
- 底部 `電子承認` 主导航在存在未处理数据时也显示未读圆点，并与顶部 `未承認` 的未读状态同步。
- 申请日与 `差戻し` 一起放在右侧信息列，申请日在上、按钮在下，并保持左边缘一致。
- 去除电子承认顶部 Header 与四个 Tab 之间露出的灰色间隙，使两个区域直接衔接。
- 首页四个业务入口使用与电子承认数据块一致的種別图标，保留首页现有图标容器尺寸、颜色和布局。
- `一括承認` 按钮在现有底部区域内稍微向下移动，按钮高度和底部导航位置保持不变。
- 电子承认画面滚动前后，固定 Header 与顶部 Tab 之间均不得出现空白间隙。
- 首页和数据一览的顶部 Header 横向拉通至手机容器左右边缘，电子承认 Header 同样保持全宽。
- ~~电子承认数据块内文字统一使用 `12px` 字号。~~ 已废止：普通正文为 `12px`，状态和种别名称使用后续单独字号；种别图标符号尺寸保持不变。
- 电子承认状态 Label 文字使用 `11px`，数据块其余文字保持 `12px`。
- `380px` 及以下小屏使用紧凑电子承认布局：卡片略收窄、列间距减小、正文与按钮使用 `11px`、状态使用 `10px`；正常尺寸仍保持原字号和布局。
- 左侧种别图标下方的种别名称统一使用 `10px` 字号，图标尺寸保持不变。
- `ホーム`、`データ一覧`、`電子承認` 三个主页面的顶部 Header 高度保持一致，并删除 Header 上方的灰色空白区域。
- 电子承认顶部四个 Tab 使用 `13px` 字号；未选中项为普通字重，选中项加粗显示。
- ~~底部主导航选中项的文字加粗显示，现有文字颜色保持不变。~~ 已废止：选中文字改为紫色；图标仍保持紫色高亮。
- 底部主导航选中项的文字使用深紫色并加粗显示，图标保持同色高亮，选中背景仍为白色。
- ~~数据块按钮区上方虚线横跨左侧种别区域和右侧内容区域。~~ 已废止：删除虚线。种别图标及名称仍保持稍微上移，尺寸不变。
- 电子承认数据块的金额数值使用深红色、`13px` 和粗体显示；金额 Label 保持原样。
- 电子承认按月显示数据时，月份栏使用与 `データ一覧` 相同的 UI：显示月份、件数和展开箭头，并支持折叠/展开当月数据。
- 电子承认顶部四个 Tab 名称保持单行显示，并按名称长度协调各 Tab 宽度，较长的 `差戻し・承認取消` 分配更宽空间。
- ~~`承認`、`差戻し` 按钮区域上方显示浅灰紫色虚线，区分数据内容区与操作区。~~ 已废止：数据块恢复为无分隔线样式。
- 电子承认数据块不显示内容区与按钮区之间的虚线分隔线。
- 电子承认顶部 Header 与四个 Tab 的白色区域无可见空隙，Tab 区域覆盖交界边缘 `1px`，滚动前后均保持贴合。
- 电子承认所有数据块左侧种别区域使用固定宽度，确保不同种别名称下的图标沿同一垂直中心线对齐。
- 增加移动端登录首屏，参考 LeySer plus 紫色品牌视觉；Logo 下显示 `学校会計` 和 `内部予算執行 スマホ版`，不显示两个站点切换 Tab。
- 登录区包含运用者名、密码、学校代码（默认 `299`）和登录按钮；登录后进入首页，退出登录后返回登录画面。
- `データ一覧` 的胶囊筛选器高度在现有基础上缩小 `1px`。
- 登录表单下方显示 `利用規約`、`はじめにお読みください`、`各サイトへのご案内` 链接，以及 `© MESCIUS inc. All Rights Reserved.` 和 MESCIUS 标识。
- 登录画面的 LeySer plus 标识保持清晰层级，`plus` 不得遮挡上方 `LeySer` 文字和彩色方块。
- 登录画面紫色品牌区底边使用双层平滑波浪线过渡到白色内容区。
- 登录画面底部链接、版权和 MESCIUS 标识之间使用更紧凑的纵向间距，当前手机高度下不显示纵向滚动条。
- 登录表单灰色容器顶部圆角完整显示，不被品牌区波浪覆盖；容器圆角使用 `24px`。
- 点击电子承认数据块进入电子承认详细画面；卡片内 `承認`、`差戻し` 按钮仍执行原操作，不触发详情跳转。
- 电子承认详情顶部灰色信息区显示 `種別`、`日付`、`番号`、`付箋：`、`電子承認状態：`。
- 详情信息区下方通过分隔标题显示详细信息，内容包含 `支払依頼書.pdf` 预览和独立印章区域；PDF 内容参考支払依頼书样式。
- 电子承认详情提供返回一览按钮，并保留详情内 `承認`、`差戻し` 操作按钮。
- 电子承认一览的每个数据块右侧显示 `›` 详情访问图标，并为图标保留独立窄列，不覆盖卡片内容。
- ~~电子承认详细画面仅隐藏顶部右上角设定图标。~~ 已废止：详情画面隐藏整个公共 Header，返回一览后恢复。
- 电子承认详细画面不显示顶部紫色 Header，从详情返回栏开始显示内容。
- 电子承认详细画面自身的标题栏使用系统主题紫色背景，标题和返回文字使用白色；标题使用普通字重，不加粗。
- 电子承认详细信息区显示 `申請日付：xxxx年xx月xx日`，日期项不增加独立背景色。
- ~~电子承认详细画面保留底部 Home、データ一覧、電子承認导航；点击底部导航时退出详情状态并正常切换页面。~~ 已废止：详情画面隐藏底部 Tab，返回其他画面后恢复。
- 独立打开 `.tmp-approval-home.html` 预览时，Home 的 `執行伺入力` 和数据一览的数据块详情入口仍可跳转到正式原型对应页面；嵌入正式原型时保持原有消息跳转方式。
- 登录成功后由外层原型保存登录状态和当前 Home 底部 Tab；从输入/详细画面返回时恢复上一个画面，不显示 Login。仅点击 Logout 时清除登录状态并返回 Login。
- Home 首页的 `執行伺入力`、`入出金入力`、`旅費申請入力`、`旅費精算入力` 四个按钮均可跳转到现有作成画面。
- ~~电子承认详细画面按参考构成重组并沿用现有系统风格：灰色信息区显示种别（如 `執行伺`）、番号、状态、付箋和電子履歴；其下白色内容区先横向显示四格电子印章，再显示 PDF 区域。~~ 印章格式已由新参考图覆盖。
- 电子承认详情的电子印章区使用三列表格：上方为窄表头行，下方为印章显示行；第一列显示红色圆形姓名/日期印章，另外两列留空，PDF 区域显示在其下方。
- 电子承认详情中付箋与上方种别/番号/状态行之间增加间距。
- 电子承认详情顶部标题使用 `13px`，详情内按钮文字统一使用 `11px`。
- 电子承认详情的印章表格降低整体高度，红色圆章和章内日期同步缩小。
- ~~电子承认详情的红色圆形印章尺寸调整为当前版本的一半（`52px` → `26px`），边框及章内文字同步缩小。~~ 已废止：需要缩小的是外层表格，不是圆章。
- 电子承认详情的红色圆形印章恢复为 `52px`；外层印章表格压缩为 `12px` 表头和 `58px` 印章行，使整体高度约为最初版本的一半并完整容纳圆章。
- 电子承认详情灰色信息区中的番号/状态行和付箋行压缩到约当前高度的一半，保留现有字体大小和两行之间的间距。
- 番号/状态行进一步固定为约 `14px`，付箋行固定为约 `12px`，避免字体自然行高重新撑大区域。
- 仅电子承认详细画面的 `承認待ち` 状态标签字号使用 `10px`；电子承认一览及其他画面保持不变。
- 电子承认详情的 `承認待ち` 标签在保持 `10px` 字号的基础上，将高度增加到 `18px`。
- 作成画面顶部标题使用普通字重（`font-weight: 400`），不加粗。
- 作成画面关闭 `貸方表示` 时，貸方面板必须完全移出布局且不保留空白；面板收起后校正滚动位置，避免内容高度缩短后出现大片空白。
- ~~作成画面的文本框、下拉框采用移动端触控样式：高度 `44px`、内容字号 `14px`、`7px` 圆角，并提供主题紫色焦点边框；textarea 最小高度 `88px`，占位文字使用 `13px`。~~ 输入框高度已调整。
- ~~作成画面的文本框、下拉框采用移动端触控样式：高度 `40px`、内容字号 `14px`、`7px` 圆角，并提供主题紫色焦点边框；textarea 最小高度 `88px`，占位文字使用 `13px`。~~ 输入框高度已再次调整。
- 作成画面的文本框、下拉框高度使用 `37px`；其他移动端输入样式保持不变。
- ~~作成画面顶部三步进度文字字号使用 `12px`。~~ 已更新为 `11px`。
- ~~作成画面顶部进度区域左右各扩展 `8px`，加宽圆点及连接线的整体分布范围。~~ 已废止：进度区域改为与 Panel 左右边缘对齐。
- ~~作成画面顶部进度文字字号使用 `11px`；首个圆点与 Panel 左边缘对齐，最后一个圆点与 Panel 右边缘对齐，连接线覆盖两端圆点之间的完整范围。~~ 首尾圆点贴边要求已废止；文字字号 `11px` 保持不变。
- ~~作成画面顶部进度条的首尾圆点分别向内收 `10px`；连接线与圆点外缘之间保留间隙，不得穿入圆圈。~~ 内缩距离已进一步调整。
- ~~作成画面顶部进度条的首尾圆点分别向内收 `16px`；连接线与圆点外缘之间保留约 `4px` 间隙，不得穿入圆圈。~~ 已废止；改为参考图的三等分居中布局。
- 作成画面顶部三个进度节点按三个等宽区域居中，文字与圆点垂直对齐；连接线停在圆点边缘外，不得伸入圆圈。
- ~~作成画面的单行输入框、下拉框高度使用 `37px`。~~ 已废止；统一缩小为 `34px`，字段下间距缩小为 `9px`。
- 作成画面字体层级统一协调：Panel 标题使用 `13px`，输入框/下拉框/文本域内容使用 `12px`，字段标签、主要操作按钮及辅助读取按钮使用 `11px`；金额合计等强调信息保留较大字号。
- ~~`執行明細` Panel 的借方明细区域增加与 `執行内容` 相同样式的 `一括ファイル読取`。~~ 按钮名称已更新。
- `執行明細` Panel 的借方明细区域增加与 `執行内容` 相同样式的 `一括ファイルから読取` 和各数据块内的 `ファイルから読取` 功能。选择文件后填入假数据；批量读取时不足 3 个数据块则自动补足至 3 个，并更新借方合计。
- 去掉 `執行明細` Panel 中 `借方明細` 标题下方的虚线；不影响 `貸方` 及其他区域的分隔样式。
- 作成画面点击顶部 `保存` 或最后一步底部 `保存` 时，先显示 `保存しました` 提示文言，再跳转到 Home 容器内的 `データ一覧` 画面；不得跳到 Login 或 Home 首页。
- 作成画面的紫色 Header 与一览画面保持一致：顶部、左侧和右侧均贴合手机画面边缘，不保留外侧空白；滚动内容高度按 `48px` Header 实际高度计算。
- 所有画面的 `‹ 戻る` 按钮尺寸统一采用作成画面标准：高度 `36px`、字号 `14px`、左右内边距 `6px`、字重 `700`；不得改变原有返回目标与跳转逻辑。
- 作成画面 `予算残高読込` 按钮的文字字号使用 `11px`，按钮高度及其他样式保持不变。
- 作成画面预算确认区域的 `すべて表示` 和预算读取后的令和日期时间文字统一使用 `12px`。
- 作成画面预算确认区域的 `部所` 信息使用普通字重 `400`，不加粗显示。
- 执行伺详细画面的字段值、番号、状态及合计值统一使用普通字重 `400`，不加粗；字段名称和区块标题保持原有层级。
- ~~电子承认详细画面的状态 Label 下方显示蓝色文字链接 `ズームイン`。~~ 已调整位置。
- 电子承认详细画面的付箋内容下方左对齐显示蓝色加粗文字链接 `ズームイン`；点击后跳转到内部予算详细画面。
- 电子承认一览数据块右上角状态 Label 统一采用电子承认详细画面状态 Label 的尺寸：高度 `18px`、内边距 `0 6px`、行高 `16px`；字号按现有详细画面层级保持 `10px/11px`。
- 电子承认一览状态按钮字号统一为 `10px`；申请番号、件名、金额/申请日三行之间使用统一标准间距：番号到件名约 `4px`，件名到金额/申请日约 `4px`。
- 电子承认一览数据块的 `承認` 按钮改为空心样式，边框和文字使用现有蓝色 `#5d99d5`；`差戻し` 按钮保持不变。
- 作成画面「様式を選択」Dialog 底部按钮采用 AI Dialog 主要操作按钮尺寸：高度 `34px`、字号 `11px`、圆角 `5px`；排列和选中按钮配色保持不变。
- 作成画面 AI 输入 Dialog 的「購入申請」「購入」快捷按钮轻微放大：高度 `24px`、字号 `10px`、内边距 `0 8px`；颜色和排列保持不变。
- 作成画面 `一括ファイルから読取` 图标的扫描框尺寸保持不变；`AI` 保持原字号并放在框内中部偏左下，叠放文件轮廓向左下方微移；单条读取图标保持不变。
- 作成画面批量读取图标内的 `AI` 在上一版基础上再向左、向下各微移 `2px`，确保文件叠放轮廓完整清晰显示；扫描框、字号和文件轮廓位置不变。
- Home 首页用户头像由灰色人物占位图替换为圆形卡通女孩头像；图片以内嵌资源保存，HTML 离线打开时仍可显示。账号菜单小头像保持不变。
- Home 首页紫色头像 Panel 比下方入力卡片更宽：头像 Panel 左右扩展至内容区边缘，下方四个入力卡片的宽度和位置保持不变。
- 作成画面的 `添付ファイル` Panel 从 `基本情報` Step 移至 `執行内容・明細` Step，并放在 `執行明細` Panel 正下方；折叠、拍照、文件选择和删除附件功能保持不变。
- 作成画面第二个 Step 名称更新为 `執行内容・明細・ファイル`；`執行内容`、`執行明細` 中选择的附件文件名统一显示在下方 `添付ファイル` Panel 内。
- 作成画面 `執行内容` 和 `執行明細` 的文件读取行数规则统一：单条 `ファイルから読取` 只处理并生成 1 行数据；`一括ファイルから読取` 固定生成 3 行数据，即使操作前已有更多数据块，读取完成后也整理为 3 行。
- 作成画面通过单条或一括文件读取生成假数据时，先显示约 `900ms` 的处理中遮罩、旋转指示和 `ファイルを読取中…` 文案，处理完成后再生成数据；处理中避免重复触发。
- ~~电子承认详细画面顶部信息区第一行、第二行保持现有标准；第二行付箋与第三行 `ズームイン` 之间使用标准 `4px` 间距。~~ 已调整为更明显的间距。
- 电子承认详细画面顶部信息区第一行、第二行保持现有标准；第二行付箋与第三行 `ズームイン` 之间使用 `8px` 间距。
- 电子承认详细画面的状态 Label 与一览画面状态 Label 使用相同尺寸和配色：最大宽度 `150px`、高度 `18px`、内边距 `0 6px`、字号 `10px`、橙色边框 `#f39a00`、浅黄色背景 `#fff6d8`、橙色文字 `#e98500`。
- 电子承认 `承認済` Tab 增加至 20 条假数据，沿用现有月份、数据块、状态和详细跳转结构；其他 Tab 不增加数据。
- 电子承认一览数据块中的 `承認` 与 `差戻し` 按钮向中间各移动 `1px`，不改变按钮尺寸和文字。
- 电子承认一览数据块中的申请番号、件名、金额/申请日三行整体向右移动 `1px`，其他区域保持不变。
- 电子承认一览数据块左侧種別图标与文字作为整体始终在数据块内垂直居中；不同数据块高度变化时保持居中。
- 电子承认 `承認済` Tab 的 20 条假数据按 4 个月分组展示：2026 年 8 月、7 月、6 月、5 月，每月 5 条。
- 电子承认详细画面底部四个操作按钮高度缩小为 `28px`，颜色、排列和文字保持不变。
- 电子承认详细画面付箋与上方状态行的间距缩小为约 `4px`；付箋与下方详细内容/操作区域的间距增加为约 `18px`，改善层次感。
- ~~电子承认一览底部的 `一括承認` 按钮在现有位置基础上向下移动 `3px`。~~ 已废止；按后续要求继续下移。
- 电子承认一览底部的 `一括承認` 按钮在当前基础上继续向下微移 `2px`（最终 `translateY(9px)`）；按钮尺寸、底部导航和功能保持不变。
- 电子承认详情的印章外层表格宽度缩小三分之一（保留 `66.666%`），并在白色内容块内右对齐。
- 电子承认详细画面的 PDF 区域下方横向显示 `承認`、`差戻し`、`付箋設定`、`付箋クリア` 四个按钮。
- 电子承认详情灰色信息区使用与电子承认一览相同的 `#f5f5f7` 背景色。
- 整个系统中的 `承認待ち` 标签统一使用浅黄色背景、橙色边框和橙色文字；已承认、差戻し、申请中状态继续使用各自现有配色。
- 详情付箋使用绿色前景色，示例内容为 `付箋：新学期　新人研究員研修費用`。
- ~~`電子履歴`、详情底部 `承認`、`付箋設定`、`付箋クリア` 使用一览承認按钮的 `#5d99d5` 蓝色；`差戻し` 使用一览差戻し按钮的红色边框和文字。~~ 已更新：蓝色按钮保持不变，差戻し改为实心红色。
- ~~电子承认一览和电子承认详情中的 `差戻し` 按钮统一使用 `#cf6570` 实心红色背景和白色文字。~~ 已废止：恢复为白底、红色边框和红色文字。
- ~~电子承认一览和电子承认详情中的 `差戻し` 按钮使用白色背景、`#cf6570` 红色边框和 `#b84f5b` 红色文字。~~ 红色色号已更新。
- ~~电子承认一览和电子承认详情中的 `差戻し` 按钮保持白色背景，边框和文字使用与电子印章一致的 `#c40010` 红色。~~ 红色色号已由用户明确更新。
- 电子承认一览和电子承认详情中的 `差戻し` 按钮保持白色背景，边框和文字使用 `#EF5B4B`。
- 电子承认详情的付箋区域不显示边框；`付箋：` 使用黑色文字，付箋内容继续使用绿色文字。
- 电子承认详情的 PDF 预览增加纵向尺寸，同时继续保持左右留白，不贯穿画面宽度。
- 电子承认详细画面的界面文字统一使用 `12px`；PDF 票据缩略内容保持自身缩放字号，避免表格内容溢出。
- 电子承认详情的印章/PDF 白色内容块左右留白，不贯穿手机画面宽度。
- 仅电子承认详细画面隐藏底部 Tab；返回电子承认一览或切换到其他画面后恢复底部 Tab，不修改其他画面的既有跳转。
