# 用户需求

## 文件

- 当前试作：`archive/internal-budget-execution-create-20260820-mobile-try.html`
- 基准存档：`archive/internal-budget-execution-create-20260819.html`
- 未经明确要求，不修改桌面 `V1.0.html`。

## 通用要求

- 页面文案全部使用日语。
- 首页、数据一览、电子承认、创建页和详情页全体统一使用 `Meiryo` 字体，并保留 `sans-serif` 回退。
- 移动端保持轻量、易填写。
- 只改用户明确提出的内容，保留已确认功能和样式。
- 每次修改后检查跳转和相关按钮。

## 当前执行内容

- 番号左侧显示。
- 上へ、下へ、複製、クリア、削除在番号行右侧横向排列。
- 下一行显示整宽虚线按钮，用于 AI 读取发票文件。
- 文件读取按钮位于番号操作行下方并横向拉宽；图标使用四角扫描框内显示 `AI` 的样式，文案为 `ファイルから読取`。
- `執行内容` 数据块上方显示与单条文件读取按钮相同样式的 `一括ファイルから読取` 按钮，并支持选择多个文件。
- `一括ファイルから読取` 的 AI 扫描图标在四角扫描框基础上增加叠放文件轮廓，表达一括读取；单条读取图标保持原样。
- 批量图标的叠放文件轮廓使用较小尺寸和紧凑错位，避免与扫描框重叠显乱。
- 批量图标的文件轮廓进一步缩小并放在扫描框内部右上角，不得超出扫描框。
- 批量图标右上角文件轮廓使用更小尺寸，并采用与主扫描框一致的深紫色。
- 一括读取附件完成后，`執行内容` 不足 3 个数据块时自动补足为 3 个，并为前 3 个数据块生成示例数据。
- 输入区域保持完整宽度。
- `基本情報` Step 的 `備考` Panel 默认折叠。
- `支払方法・支払先情報` Panel 默认折叠，`支払方法` 默认选中 `振込`。
- 样式选择对话框默认选中 `009 New1`，仍允许用户改选其他样式。
- `執行内容` Step 名称改为 `執行内容・明細`。
- 原独立 `執行明細` Step 已废止，完整内容移到 `執行内容・明細` Step 最下方。
- `執行明細` 的 `執行内容から読込` 按钮采用 `基本情報` 的 `前回内容を読込` 按钮样式，不使用白色填充背景。
- `戻る` 返回首页。

## 交付要求

- 仓库提供 `index.html` 正式入口。
- 支持 `#home`、`#create`、`#detail` 直接访问。
- 最终封装文件必须通过 Chrome 实际加载验证。

## 電子承認画面

- 首页最右侧 `電子承認` Tab 可进入独立电子承认画面。
- 画面内显示 `未承認`、`承認済`、`差戻し・承認取消`、`申請` 4 个 Tab。
- 電子承認 Tab 使用扁平文字导航样式和底部分隔线，选中项使用系统紫色文字和紫色下划线。原“整体透明背景”要求已废止。
- 電子承認顶部 Tab 区域使用白色背景并降低高度，保持紫色选中下划线。
- 電子承認顶部 Tab 白色区域横向拉宽，抵消内容区左右边距并贯穿画面宽度。~~4 个 Tab 保持等宽。~~ 已废止：按名称长度分配宽度。
- ~~電子承認顶部 4 个 Tab 的字号为 `12px`。~~ 已废止：统一改为 `13px`。
- ~~数据按月和日期分组显示。~~ 已废止：日期标题不再显示。
- 数据只显示月份分组标题，不显示月份下方的每日日期标题。
- 数据块显示件名、金额、申请日，右上角显示电子承认状态。
- 数据块第一行显示 `申請番号：xxxxx`，与右侧电子承认状态 Label 对齐；第二行显示件名；第三行并排显示金额和申请日。
- 第三行中 `金額` Label 与金额值紧挨横向排列，`申請日` Label 与日期值紧挨横向排列，两组信息保持左右分栏。
- 数据块左侧显示种别图标和名称：`執行伺`、`入出金伺`、`旅費申請`、`旅費精算`。
- 种别图标使用较小尺寸，并在数据块左侧上下垂直居中。
- 种别图标和名称与卡片左边缘保持适当距离，整体稍向右排列。
- 金额 Label 和金额值左右排列；右上角电子承认状态使用更大字号。
- 右上角电子承认状态名称使用普通字重，不加粗。
- `未承認` 数据块下方显示 `承認`、`差戻し` 按钮。
- `承認`、`差戻し` 按钮使用更小尺寸，分别使用蓝色和红色。
- 画面底部显示 `一括承認` 按钮，并支持单条及批量审批交互。
- `一括承認` 按钮使用蓝色。
- ~~画面底部 `ホーム`、`データ一覧`、`電子承認` 主导航不使用选中背景色；选中后仅图标变为系统紫色高亮，文字颜色保持一致。~~ 已废止：选中文字也改为紫色。
- 底部主导航选中图标使用清晰的深紫色和较高字重，确保不被默认灰色覆盖。
- 底部主导航使用 `Meiryo` 字体。
- 电子承认数据块内文字不使用粗体，并改为更易读的深灰色。
- ~~`承認`、`差戻し` 按钮宽度缩为原来的一半，保持现有颜色和高度并靠右显示。~~ 已废止：改为分别与左右信息列对齐。
- 画面底部主导航 Tab 高度调小，选中图标颜色及跳转功能保持不变。
- `一括承認` 按钮所在底栏不显示白色背景，按钮本身保持蓝色。
- ~~`承認` 按钮与左侧金额列左对齐，`差戻し` 按钮与右侧申请日列右对齐；两个按钮各保持原按钮的一半宽度。~~ 已废止：`差戻し` 改为在申请日所在右列下方左对齐。
- ~~电子承认数据块正文统一使用 `10px` 字号，种别图标符号维持原尺寸。~~ 已废止：数据块文字统一改为 `12px`。
- `一括承認` 按钮高度调小，宽度、颜色及批量审批功能保持不变。
- ~~`承認`、`差戻し` 按钮内文字使用 `11px` 字号。~~ 已废止：按钮文字随数据块统一改为 `12px`。
- ~~电子承认顶部四个 Tab 使用普通字重，不加粗。~~ 已废止：仅选中 Tab 加粗。
- 电子承认画面上下滚动时，顶部标题栏和四个 Tab 固定显示，仅月份及数据块区域滚动。
- ~~申请日信息在右列内右对齐，使日期值（如 `08/19`）右边缘与 `差戻し` 按钮右边缘对齐。~~ 已废止：申请日恢复原位置，仅移动按钮。
- `未承認` 存在未处理数据时显示未读圆点；全部逐条处理或一括承認后自动隐藏。
- 申请日保持原位置；`差戻し` 按钮放在申请日所在右列的正下方并左对齐。
- 底部 `電子承認` 主导航在存在未处理数据时也显示未读圆点，并与顶部 `未承認` 的未读状态同步。
- 申请日与 `差戻し` 一起放在右侧信息列，申请日在上、按钮在下，并保持左边缘一致。
- 去除电子承认顶部 Header 与四个 Tab 之间露出的灰色间隙，使两个区域直接衔接。
- 首页四个业务入口使用与电子承认数据块一致的種別图标，保留首页现有图标容器尺寸、颜色和布局。
- `一括承認` 按钮在现有底部区域内稍微向下移动，按钮高度和底部导航位置保持不变。
- 电子承认画面滚动前后，固定 Header 与顶部 Tab 之间均不得出现空白间隙。
- 首页和数据一览的顶部 Header 横向拉通至手机容器左右边缘，电子承认 Header 同样保持全宽。
- ~~电子承认数据块内文字统一使用 `12px` 字号。~~ 已废止：普通正文为 `12px`，状态和种别名称使用后续单独字号；种别图标符号尺寸保持不变。
- 电子承认状态 Label 文字使用 `11px`，数据块其余文字保持 `12px`。
- `380px` 及以下小屏使用紧凑电子承认布局：卡片略收窄、列间距减小、正文与按钮使用 `11px`、状态使用 `10px`；正常尺寸仍保持原字号和布局。
- 左侧种别图标下方的种别名称统一使用 `10px` 字号，图标尺寸保持不变。
- `ホーム`、`データ一覧`、`電子承認` 三个主页面的顶部 Header 高度保持一致，并删除 Header 上方的灰色空白区域。
- 电子承认顶部四个 Tab 使用 `13px` 字号；未选中项为普通字重，选中项加粗显示。
- ~~底部主导航选中项的文字加粗显示，现有文字颜色保持不变。~~ 已废止：选中文字改为紫色；图标仍保持紫色高亮。
- 底部主导航选中项的文字使用深紫色并加粗显示，图标保持同色高亮，选中背景仍为白色。
- ~~数据块按钮区上方虚线横跨左侧种别区域和右侧内容区域。~~ 已废止：删除虚线。种别图标及名称仍保持稍微上移，尺寸不变。
- 电子承认数据块的金额数值使用深红色、`13px` 和粗体显示；金额 Label 保持原样。
- 电子承认按月显示数据时，月份栏使用与 `データ一覧` 相同的 UI：显示月份、件数和展开箭头，并支持折叠/展开当月数据。
- 电子承认顶部四个 Tab 名称保持单行显示，并按名称长度协调各 Tab 宽度，较长的 `差戻し・承認取消` 分配更宽空间。
- ~~`承認`、`差戻し` 按钮区域上方显示浅灰紫色虚线，区分数据内容区与操作区。~~ 已废止：数据块恢复为无分隔线样式。
- 电子承认数据块不显示内容区与按钮区之间的虚线分隔线。
- 电子承认顶部 Header 与四个 Tab 的白色区域无可见空隙，Tab 区域覆盖交界边缘 `1px`，滚动前后均保持贴合。
